# AI 協作規則（Claude / GPT 通用）

> 回覆一律使用**繁體中文**。

---

## 唯一真實來源是 `CLAUDE.md`

本專案的所有 AI 協作規則 —— 目錄結構、架構規則、樣式規則（**亮色白底主題**）、
品牌色、程式碼風格、資料庫規則、禁止清單、測試規範、安全與金鑰處理、
行程抓取規範等 —— 全部集中在專案根目錄的 **`CLAUDE.md`**。

**要把規則貼給 GPT 或其他 AI 時，請貼 `CLAUDE.md` 的內容，不要用這個檔案。**

### 為什麼這個檔案只剩一句話

這裡曾經是一份完整的規則副本。但一份規則維護成兩個檔案，兩邊必然逐漸分岔：
到 2026-07 時，這個檔案還寫著早已淘汰的「深色毛玻璃主題」（網站早就改成亮色
白底）、只列了 2 個頁面與 7 個 API（實際已有 20+ 頁、50+ API），還提到已刪除的
`ImageEditor` 與根本不存在的 `InquiryForm` —— 照它做只會產出跟現況完全不搭的結果。

所以現在只留一個指標。規則只有一份（`CLAUDE.md`），就不會再有「改了一邊、
忘了另一邊」的問題。
